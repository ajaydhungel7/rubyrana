name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby-version: ["3.4"]
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler: "2.4.22"
          bundler-cache: true
      - name: Run tests
        run: bundle exec ruby -Itest -e "Dir['test/**/*_test.rb'].each { |f| require_relative f }"

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          bundler: "2.4.22"
          bundler-cache: true
      - name: Run RuboCop
        run: bundle exec rubocop
